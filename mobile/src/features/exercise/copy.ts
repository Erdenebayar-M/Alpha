// Copy shared across renderer files (as opposed to per-task content, which lives on
// the Task itself). Follows the shared-labels precedent set by
// src/features/plan/planFormat.ts.

// Accessibility label for every tappable "play the prompt audio" target — the
// dedicated SpeakerButton and the larger header rows that also toggle audio on tap.
export const LISTEN_LABEL = 'Сонсох';
